import React from "react";
import styles from "../../styles/SuperText.module.css";

const SuperText = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.heading}>Gradient Text</h1>
    </div>
  );
};

export default SuperText;
